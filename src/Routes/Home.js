import React from "react";
import Hero from "../components/Hero";

const Home = () => {
  return (
    <div>
      <Hero /> {/* Use the Hero component here */}
    </div>
  );
};

export default Home;
